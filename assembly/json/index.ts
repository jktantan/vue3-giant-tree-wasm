/**
 * JSON 工具模块：轻量级编码器 + 字符串转义
 * JSON utilities module: lightweight encoder + string escaping
 * Модуль утилит JSON: лёгкий кодировщик + экранирование строк
 */
export { JsonEncoder } from './serializer'

export { escapeString } from './types'
